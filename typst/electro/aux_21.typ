#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ epsilon.alt = -frac(d Phi, d t), quad integral.cont_Gamma arrow(E) dot.op d arrow(l) = -frac(partial, partial t) integral.double_S arrow(B) dot.op d arrow(S), quad nabla times arrow(E) = -frac(partial arrow(B), partial t) $

#pagebreak()

$ Phi = integral.double_S arrow(B) dot.op d arrow(S) $

#pagebreak()

$ epsilon.alt = -C pi a^2 v, quad I = frac(epsilon.alt, R) = -frac(C pi a^2 v, R), quad F_z = -frac(C^2 pi^2 a^4, R) v $

#pagebreak()

$ Phi_2 = M I_1, quad Phi = L I, quad M_(12) = M_(21) = M, quad L_"sol" = frac(mu_0 N^2 pi r^2, h) $

#pagebreak()

$ cal(E)_2 = -M frac(d I_1, d t), quad cal(E)_1 = -M frac(d I_2, d t), quad cal(E) = -L frac(d I, d t) $
