#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ epsilon = -frac(d Phi, d t), quad Phi = integral.double_S bold(B) dot d bold(S) $

#pagebreak()

$ bold(B) = B_0 hat(z), quad epsilon = B_0 l v, quad I = frac(B_0 l v, R), quad bold(F) = -frac(B_0^2 l^2, R) v hat(x), quad v(t) = v_0 e^(-t\/tau), quad tau = frac(m R, B_0^2 l^2) $

#pagebreak()

$ Phi_2 = M I_1, quad epsilon_2 = -M frac(d I_1, d t), quad M = frac(B_0 l x, I_0); quad Phi = L I, quad epsilon = -L frac(d I, d t) $

#pagebreak()

$ bold(B) = mu_0 n I(t) hat(z), quad I(t) = I_0 sin(omega t) \
bold(E)(r < b) = -frac(mu_0 n I_0 omega, 2) r cos(omega t) hat(phi) \
bold(E)(r > b) = -frac(mu_0 n I_0 omega b^2, 2r) cos(omega t) hat(phi) $

#pagebreak()

$ L frac(d I, d t) + R I = B_0 l v(t) = B_0 l a t $
