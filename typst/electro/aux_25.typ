#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$bold(H) = 1/mu_0 bold(B) - bold(M) quad arrow.r.double quad integral.cont_Gamma bold(H) dot d arrow(l) = I_"enc"^"libre"$

#pagebreak()

$bold(J)_m = nabla times bold(M) quad quad bold(K)_m = bold(M) times hat(n)$

#pagebreak()

$bold(B) = mu bold(H), quad mu = mu_0 (1 + chi_m) = mu_0 mu_r, quad mu_r = mu / mu_0 = 1 + chi_m$

#pagebreak()

$bold(H) = N I / (2 pi r) hat(phi) quad arrow.r.double quad bold(B)(r, theta) = mu(theta) (N I) / (2 pi r) hat(phi)$

#pagebreak()

$L = (N^2 h) / (2 pi) integral_0^(2 pi) mu(theta) / 2 d theta dot.op ln(b / a) = mu_0 chevron.l mu_r chevron.r (N^2 h) / (2 pi) ln(b / a)$
