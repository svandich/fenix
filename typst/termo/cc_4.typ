#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ p V^gamma = "cte" quad quad gamma = C_p / C_V = (f + 2) / f $

#pagebreak()

$ W_"ad" = N C_V (T_f - T_i) = (p_f V_f - p_i V_i) / (1 - gamma) $

#pagebreak()

$ v_s = sqrt(gamma p \/ rho) = sqrt(gamma k_B T \/ m) $
