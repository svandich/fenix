#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ f(v_x) = sqrt(m / (2 pi k_B T)) exp(-(m v_x^2) / (2 k_B T)) $

#pagebreak()

$ f(v) = 4 pi n (m / (2 pi k_B T))^(3/2) v^2 exp(-(m v^2) / (2 k_B T)) $

#pagebreak()

$ v_p = sqrt((2 k_B T)/m) quad chevron.l v chevron.r = sqrt((8 k_B T)/(pi m)) quad v_"rms" = sqrt((3 k_B T)/m) $

#pagebreak()

$ r_1 / r_2 = sqrt(m_2 / m_1) $
