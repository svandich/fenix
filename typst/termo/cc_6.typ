#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ d S = (macron(delta) Q_"rev") / T quad quad integral.cont (macron(delta) Q) / T <= 0 quad quad Delta S_"univ" >= 0 $

#pagebreak()

$ S = k_B ln Omega quad quad Omega = N! / (product_i N_i !) $

#pagebreak()

$ S = N k_B lr([5/2 + ln lr((V / (N lambda_"th"^3)))]) $
