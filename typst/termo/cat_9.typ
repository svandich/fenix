#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ W = Q_c - Q_f quad "con" quad Q_c > 0, Q_f > 0, W > 0 $

#pagebreak()

$ eta = W/Q_c = 1 - Q_f/Q_c < 1 $

#pagebreak()

$ Q_c = Q_f + W quad => quad "COP" = Q_f/W = Q_f/(Q_c - Q_f) $
