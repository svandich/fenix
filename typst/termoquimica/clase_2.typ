#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ Z = overline(V)_"real"/overline(V)_"ideal" = (P overline(V))/(R T) quad quad Z_"gas ideal" = 1 $

#pagebreak()

$ overline(V) = b + (R T)/P quad => quad Z = 1 + (b P)/(R T) quad quad (P -> infinity: overline(V) -> b) $

#pagebreak()

$ P_"interna" prop 1/overline(V)^2 quad quad P = (R T)/(overline(V) - b) - a/overline(V)^2 $

#pagebreak()

$ (P + a/overline(V)^2)(overline(V) - b) = R T quad quad (P + (n^2 a)/V^2)(V - n b) = n R T $

#pagebreak()

$ [a] = "L"^2 dot.op "atm" dot.op "mol"^(-2) quad quad [b] = "L" dot.op "mol"^(-1) $
