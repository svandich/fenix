#set page(width: auto, height: auto, margin: (x: 0.5em, y: 0.3em), fill: none)
#set text(fill: rgb("#e6edf3"), size: 13pt)

$ T_A = T_C quad "y" quad T_B = T_C quad => quad T_A = T_B quad quad ("la ley cero define " T) $

#pagebreak()

$ W = P_"op" A h = P_"op" (V_2 - V_1) = P_"op" Delta V $

#pagebreak()

$ W = P'_"op" (V' - V_1) + P''_"op" (V_2 - V') quad quad W_(2 "etapas") > W_(1 "etapa") $

#pagebreak()

$ macron(delta) W = P_"op" thin d V quad => quad W = integral_(V_1)^(V_2) P_"op" thin d V $

#pagebreak()

$ P_"op" = P_"sist" quad => quad W_"máx" = integral_(V_i)^(V_f) (n R T)/V thin d V = n R T ln V_f/V_i $

#pagebreak()

$ W_"ciclo" = (P_2 - P_1)(V_2 - V_1) < 0 quad quad ("un solo paso" => "IRREVERSIBLE") $

#pagebreak()

$ W_"ciclo" = integral_(V_1)^(V_2) P thin d V + integral_(V_2)^(V_1) P thin d V = 0 quad quad ("cuasiestático" => "REVERSIBLE") $
